# Brandon's Terminal Portfolio

A portfolio you can SSH into.

## Local run

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python portfolio.py
# or: textual run --dev portfolio.py